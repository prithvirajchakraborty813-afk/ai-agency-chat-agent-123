# Secrets — never commit these. Set as environment variables on Render instead.
.env
*.env

# Old local JSON state files — superseded by Postgres (db_storage.py).
# If these still exist from before the Postgres migration, they must NOT
# be pushed to GitHub: conversations.json/orders.json may contain real
# customer phone numbers, order amounts, and scope details.
conversations.json
orders.json
incoming_log.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# Lead and proposal data - contains real business info and pricing
qualified.csv
prospects.csv
prospects_maps.csv
prospects_merged.csv
prospects_clinics_salons.csv
proposals.csv
proposals.xlsx
debug_test.csv
